node_modules
dist
.env
.env.local
.env.*.local
*.log
.DS_Store

.vercel

# Local secrets / OAuth credentials — never commit
client_secret_*.json
Ignore.txt
.claude/

# Capacitor / Android build artifacts (keep android/ itself, ignore builds)
android/app/build/
android/build/
android/.gradle/
android/app/release/
android/local.properties
*.keystore
*.jks
google-services.json

# Supabase CLI cache — contains a live DB connection string
supabase/.temp/
